@model IEnumerable<perfume.Models.Favorite>

@{
    ViewData["Title"] = "Favorite";
}
<div class="d-flex justify-content-center align-content-center" style="margin-top:100px;" >
    <h3>My Favorites</h3>

</div>

@*<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Image)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                    <img src="../@Html.DisplayFor(modelItem => item.Image)" alt="@item.Name" class="img-fluid">
                
            </td>
            <td>
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

*@
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-lg-4 col-md-6 mb-5">
            <div class="card shadow-sm">
                <img src="../@item.Image" alt="@item.Name" class="card-img-top" style="height:200px;object-fit:cover;">
                <div class="card-body">
                    <h5 class="card-title">@item.Name</h5>
                    <div class="d-flex justify-content-end" style="gap:5px;">
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger ml-1">Delete</a>
                        <a asp-action="payNaw" asp-route-Name="@item.Name" class="btn btn-primary ml-1">Pay Now</a>

                    </div>
            
                </div>
            </div>
        </div>
    }
</div>
